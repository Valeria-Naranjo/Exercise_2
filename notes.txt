para interactuar con una api te pide que pongas el método, la ruta y liego la ainformación 

todo lo que envía el front end va encriptados
todos los servidosres traen su desencriptador de HTTPS
es muy seguro enviat iformación a los servidosres HTTPS 
y por eso no es necesario encriptar en el front

4 tipos de arquitectura most used
Event driven
MVC
Micriservicios
Monilitica}}

TRY CATCH captura errores en las apps
un MODELO es una representación de una tabla en JS



TAREA
app.get
app.select

SELECT * = FIND ()
SELECT * WHERE ID = FIND(PARAMETRO)


TO GET ALL CARDS 
getcard/? 'D


el archivo env literalmemte guarde el environmpent de proyecto 
y ese NO se sube a github, porque es dar las credenciales para cambiar las variable de entorno
